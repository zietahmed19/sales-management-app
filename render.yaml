version: "1"
build:
  commands:
    - echo "Building backend server..."
    - npm install
    - mkdir -p data
services:
  - type: node
    name: sales-backend
    env:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    buildCommand: npm install
    startCommand: node server-db.js
